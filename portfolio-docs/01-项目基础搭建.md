# Step 1: 项目基础搭建

## 1.1 技术栈

| 层级 | 技术 | 版本 |
|-----|------|------|
| 前端框架 | React | 18.x |
| 类型安全 | TypeScript | 5.x |
| 构建工具 | Vite | 5.x |
| UI组件 | shadcn/ui | latest |
| 样式 | Tailwind CSS | 3.x |
| 数据获取 | React Query | 5.x |
| 状态管理 | Zustand | 4.x |
| 路由 | React Router | 6.x |
| 动画 | framer-motion | 11.x |
| 后端语言 | Go | 1.21+ |
| Web框架 | Gin | 1.9+ |
| ORM | GORM | 1.25+ |
| 数据库 | PostgreSQL | 15+ |
| 向量扩展 | pgvector | 0.6+ |
| 缓存 | Redis | 7.x |
| 文件存储 | MinIO | latest |

## 1.2 项目目录结构

```
portfolio/
├── frontend/                    # 前端项目
│   ├── public/
│   ├── src/
│   │   ├── components/          # 通用组件
│   │   │   ├── ui/              # shadcn/ui 组件
│   │   │   ├── layout/          # 布局组件
│   │   │   └── shared/          # 共享组件
│   │   ├── pages/               # 页面
│   │   │   ├── home/            # 首页
│   │   │   ├── projects/        # 作品
│   │   │   ├── agent/           # Agent
│   │   │   ├── booking/         # 预约
│   │   │   └── admin/           # 管理后台
│   │   ├── hooks/               # 自定义Hooks
│   │   ├── lib/                 # 工具函数
│   │   ├── stores/              # Zustand状态
│   │   ├── types/               # TypeScript类型
│   │   ├── api/                 # API请求封装
│   │   ├── styles/              # 全局样式
│   │   ├── App.tsx
│   │   └── main.tsx
│   ├── tailwind.config.ts
│   ├── tsconfig.json
│   ├── vite.config.ts
│   └── package.json
├── backend/                     # 后端项目
│   ├── cmd/
│   │   └── server/main.go       # 入口
│   ├── internal/
│   │   ├── config/              # 配置
│   │   ├── middleware/          # 中间件
│   │   ├── handler/             # 处理器
│   │   ├── service/             # 业务逻辑
│   │   ├── repository/          # 数据访问
│   │   ├── model/               # 数据模型
│   │   ├── router/              # 路由
│   │   └── pkg/                 # 工具包
│   ├── migrations/              # 数据库迁移
│   ├── docs/                    # Swagger文档
│   ├── go.mod
│   └── go.sum
├── docker-compose.yml           # Docker编排
├── docker-compose.dev.yml       # 开发环境
├── nginx/                       # Nginx配置
│   └── nginx.conf
├── scripts/                     # 脚本
│   ├── init-db.sql
│   └── seed-data.sql
└── README.md
```

## 1.3 Docker Compose 开发环境

```yaml
# docker-compose.dev.yml
version: '3.8'
services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: portfolio_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: devpassword
    ports:
      - "5432:5432"
    volumes:
      - postgres_dev:/var/lib/postgresql/data
      - ./scripts/init-db.sql:/docker-entrypoint-initdb.d/01-init.sql

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_dev:/data

  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio_dev:/data

volumes:
  postgres_dev:
  redis_dev:
  minio_dev:
```

## 1.4 数据库初始化

```sql
-- scripts/init-db.sql

-- 启用扩展
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pgvector";

-- 创建数据库（如需要）
-- CREATE DATABASE portfolio_dev;
```

## 1.5 前端初始化

```bash
# 创建React项目
npm create vite@latest frontend -- --template react-ts
cd frontend

# 安装依赖
npm install react-router-dom @tanstack/react-query zustand framer-motion
npm install -D tailwindcss postcss autoprefixer
npm install -D eslint prettier eslint-config-prettier

# 初始化Tailwind
npx tailwindcss init -p

# 初始化shadcn/ui
npx shadcn-ui@latest init
```

## 1.6 后端初始化

```bash
# 创建Go项目
mkdir -p backend && cd backend
go mod init portfolio-backend

# 安装依赖
go get -u github.com/gin-gonic/gin
go get -u github.com/gin-contrib/cors
go get -u gorm.io/gorm
go get -u gorm.io/driver/postgres
go get -u github.com/golang-jwt/jwt/v5
go get -u github.com/minio/minio-go/v7
go get -u github.com/redis/go-redis/v9
go get -u golang.org/x/crypto/bcrypt

# Swagger文档
go get -u github.com/swaggo/swag/cmd/swag
go get -u github.com/swaggo/gin-swagger
go get -u github.com/swaggo/files
```

## 1.7 开发工具配置

### ESLint + Prettier

```json
// .eslintrc.json
{
  "extends": [
    "eslint:recommended",
    "plugin:@typescript-eslint/recommended",
    "prettier"
  ],
  "rules": {
    "no-unused-vars": "warn",
    "@typescript-eslint/no-explicit-any": "warn"
  }
}
```

### Git Hooks

```json
// package.json
{
  "lint-staged": {
    "*.{ts,tsx}": ["eslint --fix", "prettier --write"],
    "*.{json,md}": ["prettier --write"]
  }
}
```

## 1.8 环境变量

```bash
# frontend/.env.development
VITE_API_BASE_URL=http://localhost:8080/api
VITE_UPLOAD_URL=http://localhost:9000

# backend/.env.development
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=devpassword
DB_NAME=portfolio_dev
REDIS_HOST=localhost
REDIS_PORT=6379
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
JWT_SECRET=dev-jwt-secret-key
JWT_EXPIRATION=168h
```

## 1.9 验收标准

- [ ] `docker-compose -f docker-compose.dev.yml up` 可启动所有基础服务
- [ ] 前端 `npm run dev` 可访问 http://localhost:3000
- [ ] 后端 `go run cmd/server/main.go` 可访问 http://localhost:8080/api/health
- [ ] PostgreSQL连接正常，pgvector扩展已启用
- [ ] Redis连接正常
- [ ] MinIO控制台可访问 http://localhost:9001
- [ ] ESLint + Prettier 配置生效
